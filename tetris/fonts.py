import pygame
from pygame.font import Font

pygame.font.init()

# 创建若干 Font 对象，参数为字体文件路径和字体大小
small_font = Font(None, 20)  # 小字体
regular_font = Font(None, 26)  # 普通字体
large_font = Font(None, 32)  # 大字体
